const ProductDescription = ({ description }) => {
  return (
    <div className="mt-8">
      <p className="text-custom-p whitespace-pre-line">{description}</p>
    </div>
  );
};

export default ProductDescription;
